# Marketing site UI kit

A single-page marketing site for つなぐProject — calm, editorial, bilingual.

## Components
- `Mark` — the dot-and-thread brand mark, accepts `size` and `accent`.
- `Header` — sticky bilingual nav, hairline border on scroll.
- `Hero` — full-bleed thread-field background, JP-primary headline with thread accent.
- `Bridge` — the signature two-pane "today / tomorrow" layout joined by a thread.
- `Practice` — three-up dark section listing the three伴走 practices.
- `Notes` — three-up cards linking to note.com posts.
- `Footer` — four-column footer + bottom meta strip.

Open `index.html` to see them composed.
